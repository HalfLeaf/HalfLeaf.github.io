"use strict";
const reading_time_1 = require("./reading-time");
module.exports = (options, { themeConfig }) => ({
    name: "reading-time",
    extendPageData($page) {
        $page.readingTime = reading_time_1.default(
        // eslint-disable-next-line no-underscore-dangle
        $page._strippedContent, options.wordPerminute || themeConfig.wordPerminute || 300);
    },
});
