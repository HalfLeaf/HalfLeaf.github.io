"use strict";
const path_1 = require("path");
module.exports = ({ container = ".theme-default-content", selector = ".theme-default-content :not(a) > img", option = {}, }) => ({
    name: "photo-swipe",
    define: {
        // eslint-disable-next-line @typescript-eslint/naming-convention
        IMAGE_CONTAINER: container,
        // eslint-disable-next-line @typescript-eslint/naming-convention
        IMAGE_SELECTOR: selector,
        // eslint-disable-next-line @typescript-eslint/naming-convention
        PHOTOSWIPE_OPTIONS: option,
    },
    enhanceAppFiles: path_1.resolve(__dirname, "enhanceAppFile.ts"),
    plugins: [
        /** typescript support */
        ["typescript"],
    ],
    globalUIComponents: "PhotoSwipeUI",
});
